name: Test
on:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Fail Test
        run: exit 1

      - name: Pass Test
        if: ${{ failed() or success() }}
        run: exit 0

      - name: Run Anyway
        if: ${{ failed() or success() }}
        run: echo "Hello"
